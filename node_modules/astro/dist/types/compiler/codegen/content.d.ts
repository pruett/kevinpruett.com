/**
 * Handling for import.meta.glob and import.meta.globEager
 */
interface GlobOptions {
    namespace: string;
    filename: string;
    projectRoot: URL;
}
interface GlobResult {
    /** Array of import statements to inject */
    imports: Set<string>;
    /** Replace original code with */
    code: string;
}
/** Astro.fetchContent() */
export declare function fetchContent(spec: string, { namespace, filename }: GlobOptions): GlobResult;
export {};
