export interface PageMeta {
    /** (required) The canonical URL of the page */
    canonicalURL: string;
}
/** Construct sitemap.xml given a set of URLs */
export declare function generateSitemap(pages: PageMeta[]): string;
